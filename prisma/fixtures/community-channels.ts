import { CommunityChannel } from '@prisma/client';

const communitiesChannels: CommunityChannel[] = [
  {
    id: 1,
    channelTypeId: 1,
    communityId: 1,
  },
];

export default communitiesChannels;
